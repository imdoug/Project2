<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>INDEX PAGE</title>
    </head>
    <body>
        <form action="/sessions?_method=DELETE" method="POST">
            <h1>RESTAURANT INDEX PAGE </h1>
            <a href="/users" class="signup">SIGN UP</a>
            <a href="/sessions" class="signup">LOGIN</a>
            <% if (currentUser) {%>
                <input type="submit" value="LOG OUT"/>
            <% } %>
            <ul>
                <% for(let i = 0; i < dishes.length; i++){ %>
                    <li><a href="/restaurant/<%=dishes[i].id%>"><%=dishes[i].name%></a></li>
                    <% } %>
            </ul>
            <nav>
                <a href="/restaurant/new">create new item</a>
            </nav>
        </form>
    </body>
</html>